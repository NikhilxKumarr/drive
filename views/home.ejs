<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <link
      href="https://cdn.jsdelivr.net/npm/flowbite@4.0.1/dist/flowbite.min.css"
      rel="stylesheet"
    />
  </head>

  <body class="bg-gray-100 min-h-screen">

    <div class="p-6">
      <button
        onclick="showPopup()"
        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg shadow hover:bg-blue-700 transition"
      >
        Upload File
      </button>
    </div>

    <main
      id="uploadModal"
      class="hidden fixed inset-0 bg-black/40 backdrop-blur-sm flex items-center justify-center z-50 px-4"
    >
      <div class="w-full max-w-md bg-white rounded-xl shadow-xl p-6 relative">

        <button
          onclick="hidePopup()"
          class="absolute top-3 right-3 text-gray-400 hover:text-gray-600"
        >
          ✕
        </button>

        <div class="text-center mb-6">
          <h1 class="text-2xl font-bold text-gray-800">
            Upload your file
          </h1>
          <p class="text-sm text-gray-500 mt-1">
            Securely store your files in Drive 🚀
          </p>
        </div>

        <form
          action="/upload-file"
          method="post"
          enctype="multipart/form-data"
          class="space-y-5"
        >
          <label
            for="file"
            class="flex flex-col items-center justify-center h-56 border-2 border-dashed border-gray-300 rounded-lg cursor-pointer hover:bg-gray-50 transition"
          >
            <svg
              class="w-10 h-10 mb-3 text-gray-400"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              stroke-width="2"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 16v-8m0 0-3 3m3-3 3 3M20 16.5A4.5 4.5 0 0 0 15.5 12h-.35A6 6 0 1 0 6 18h8"
              />
            </svg>

            <p class="text-sm text-gray-600">
              <span class="font-medium">Click to upload</span> or drag & drop
            </p>
            <p class="text-xs text-gray-400 mt-1">
              PNG, JPG, PDF (Max 10MB)
            </p>

            <input id="file" type="file" name="file" class="hidden" />
          </label>

          <button
            type="submit"
            class="w-full py-2.5 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700 transition"
          >
            Upload File
          </button>
        </form>
      </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/flowbite@4.0.1/dist/flowbite.min.js"></script>

    <script>
      function showPopup() {
        document.getElementById("uploadModal").classList.remove("hidden");
      }

      function hidePopup() {
        document.getElementById("uploadModal").classList.add("hidden");
      }
    </script>
  </body>
</html>
